Folder ini dikosongkan di-git. Isi saat runtime:
- chroma_db/   → Vector store ChromaDB (auto-generated saat ingest)
- docs/         → File PDF/DOCX SOP yang di-upload

Pastikan folder ini di-mount sebagai Docker Volume agar data persisten.
